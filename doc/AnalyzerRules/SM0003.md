# SM0003 : Unsupported feature (Error)

The target contained a feature that is explicitly not supported.

- Method returning a ref result.
- 

```csharp
    public interface IRefMethod
    {
        ref string GetName();
    }
```
